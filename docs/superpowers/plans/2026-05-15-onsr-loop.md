# ONSR Auto-Learning Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the Observe-Notice-Store-Recall (ONSR) auto-learning loop to all five cc-content skills so corrections from one session automatically inform the next, without conflicting with cc-config or other plugins.

**Architecture:** Each skill file gets a new Step 0 (silent recall of `.claude/learnings.md`) and a store phase at the end (auto-append qualifying observations before explicit feedback). `cc-content-session-wrap`'s pattern-detection step is scoped to `[cc-content:*]` tags only. `cc-content-new-skill`'s SKILL.md skeleton template is updated so every skill it generates inherits the full ONSR pattern automatically.

**Tech Stack:** Markdown skill files only. No automated tests — verification is done via `grep` to confirm text presence and structure.

**Spec:** `docs/superpowers/specs/2026-05-15-onsr-loop-design.md`

---

## File Map

| File                                                             | Change                                                                |
| ---------------------------------------------------------------- | --------------------------------------------------------------------- |
| `plugins/cc-content/skills/cc-content-linkedin-post/SKILL.md`    | Insert Step 0; replace Step 5                                         |
| `plugins/cc-content/skills/cc-content-onboarding/SKILL.md`       | Insert Step 0; add Step 8                                             |
| `plugins/cc-content/skills/cc-content-samples-curation/SKILL.md` | Insert Step 0; modify Step 5 No-branch; add Step 6                    |
| `plugins/cc-content/skills/cc-content-session-wrap/SKILL.md`     | Insert Step 0; add scoping line to Step 5                             |
| `plugins/cc-content/skills/cc-content-new-skill/SKILL.md`        | Insert Step 0; update Step 0 and Step 8 descriptions in skeleton spec |

---

### Task 1: cc-content-linkedin-post — Add Step 0 and replace Step 5

**Files:**

- Modify: `plugins/cc-content/skills/cc-content-linkedin-post/SKILL.md`

- [ ] **Step 1: Read the file**

```bash
cat -n plugins/cc-content/skills/cc-content-linkedin-post/SKILL.md
```

Confirm the file starts with the YAML frontmatter block and that `## Step 1: Load context` and `## Step 5: Feedback` are present.

- [ ] **Step 2: Insert Step 0 before Step 1**

Using the Edit tool, insert the following block immediately before the line `## Step 1: Load context`:

```
## Step 0: Recall learnings

If `.claude/learnings.md` exists, read it silently. Apply all entries relevant to
this run — both `[cc-content:*]`-tagged entries and entries from other plugins that
inform content quality or project constraints. Do not announce this step. If the file
is absent, continue normally.

```

(Leave one blank line between the new block and `## Step 1`.)

- [ ] **Step 3: Replace Step 5 entirely**

Using the Edit tool, replace everything from `## Step 5: Feedback` to the end of the file with:

````
## Step 5: Feedback

**Auto-store phase.** Before asking for feedback, review this run. For each qualifying
observation, append one tagged line to `.claude/learnings.md` (create with standard
header if missing):

```text
[cc-content:cc-content-linkedin-post] <concise observation> — <YYYY-MM-DD>
````

Qualifies: content preferences or constraints not already in any loaded `context/` file
or `CLAUDE.md`; corrections the owner made to the output; project-specific facts that
would change future output; accepted/rejected suggestions deviating from best practices.

Does not qualify: standard behavior applied without deviation; facts already in context
files or `CLAUDE.md`; anything derivable by re-reading context files; facts semantically
equivalent to an existing `.claude/learnings.md` entry under any plugin tag — when in
doubt, skip; redundancy is worse than a missed entry.

Check for the file before appending:

```bash
ls .claude/learnings.md 2>/dev/null && echo "exists" || echo "missing"
```

Standard header when creating the file:

```markdown
# Learnings

Corrections and feedback collected during content sessions.
Entries are tagged by skill and dated.

---
```

**Explicit feedback.** After the auto-store phase, ask:

> "Did this post meet expectations? If you have any corrections or notes for
> future posts, share them here — or press Enter to finish."

- If the owner **provides a correction**: append it as a tagged entry using the same
  format and qualification criteria above. Confirm total entries written across both
  phases: "✓ N learning(s) saved to `.claude/learnings.md`."
- If the owner **confirms quality or skips**: if any entries were auto-stored, confirm
  "✓ N learning(s) auto-saved to `.claude/learnings.md`." Then say "Great — the post
  is ready to publish. Copy it above and paste directly into LinkedIn." and exit. If
  nothing was stored, skip the confirmation and exit directly.

````

- [ ] **Step 4: Verify**

```bash
grep -n "Step 0\|Auto-store phase\|Explicit feedback" \
  plugins/cc-content/skills/cc-content-linkedin-post/SKILL.md
````

Expected: lines for `## Step 0: Recall learnings`, `**Auto-store phase.**`, and `**Explicit feedback.**` all present.

- [ ] **Step 5: Commit**

```bash
git add plugins/cc-content/skills/cc-content-linkedin-post/SKILL.md
git commit -m "feat: ✨ add ONSR loop to cc-content-linkedin-post"
```

---

### Task 2: cc-content-onboarding — Add Step 0 and add Step 8

**Files:**

- Modify: `plugins/cc-content/skills/cc-content-onboarding/SKILL.md`

- [ ] **Step 1: Read the file**

```bash
cat -n plugins/cc-content/skills/cc-content-onboarding/SKILL.md
```

Confirm `## Step 1: Discover existing context` and `## Step 7: Summary` are present and that the file ends after Step 7.

- [ ] **Step 2: Insert Step 0 before Step 1**

Using the Edit tool, insert the following block immediately before `## Step 1: Discover existing context`:

```
## Step 0: Recall learnings

If `.claude/learnings.md` exists, read it silently. Apply relevant entries — in
particular, prior path preferences, rejected interview answers, or project constraints
that would change how questions are asked this run. Do not announce this step. If the
file is absent, continue normally.

```

(Leave one blank line between the new block and `## Step 1`.)

- [ ] **Step 3: Append Step 8 after Step 7**

Using the Edit tool, append the following block at the end of the file (after the closing paragraph of Step 7):

````

## Step 8: Store learnings

Review the interview conducted in Steps 2–5. For each nuance or constraint the owner
revealed that was NOT formalized into any context file created during this run, append
one tagged line to `.claude/learnings.md` (create with standard header if missing):

```text
[cc-content:cc-content-onboarding] <concise fact> — <YYYY-MM-DD>
````

Before appending a candidate fact, explicitly verify it is not present in any context
file written during Steps 2–5 of this run. Also check whether a semantically equivalent
entry already exists in `.claude/learnings.md` under any plugin tag — if so, skip (when
in doubt, skip).

Qualifies: project-specific nuances too narrow to fit neatly into a context file (e.g.,
"client avoids competitor comparisons"); path preferences or rejected options that would
change how this skill behaves on re-run (e.g., "client declined to set up
target-audience file"); constraints discovered during the interview that would change
future skill behavior.

Does not qualify: facts now captured in any context file written during this run; facts
already in `CLAUDE.md`; facts semantically equivalent to an existing `.claude/learnings.md`
entry under any plugin tag.

Check for the file before appending:

```bash
ls .claude/learnings.md 2>/dev/null && echo "exists" || echo "missing"
```

Standard header when creating the file:

```markdown
# Learnings

Corrections and feedback collected during content sessions.
Entries are tagged by skill and dated.

---
```

Confirm any entries written: "✓ N learning(s) saved to `.claude/learnings.md`." If no
qualifying observations are found, proceed silently.

````

- [ ] **Step 4: Verify**

```bash
grep -n "Step 0\|Step 8\|Store learnings" \
  plugins/cc-content/skills/cc-content-onboarding/SKILL.md
````

Expected: lines for `## Step 0: Recall learnings` and `## Step 8: Store learnings` both present.

- [ ] **Step 5: Commit**

```bash
git add plugins/cc-content/skills/cc-content-onboarding/SKILL.md
git commit -m "feat: ✨ add ONSR loop to cc-content-onboarding"
```

---

### Task 3: cc-content-samples-curation — Add Step 0, update Step 5, add Step 6

**Files:**

- Modify: `plugins/cc-content/skills/cc-content-samples-curation/SKILL.md`

- [ ] **Step 1: Read the file**

```bash
cat -n plugins/cc-content/skills/cc-content-samples-curation/SKILL.md
```

Confirm `## Step 1: Identify the content` and `## Step 5: Loop` are present and that Step 5's No-branch currently says `say "Samples curation complete…"`.

- [ ] **Step 2: Insert Step 0 before Step 1**

Using the Edit tool, insert the following block immediately before `## Step 1: Identify the content`:

```
## Step 0: Recall learnings

If `.claude/learnings.md` exists, read it silently. Apply all entries relevant to
this run. Do not announce this step. If the file is absent, continue normally.

```

(Leave one blank line between the new block and `## Step 1`.)

- [ ] **Step 3: Update the No-branch in Step 5**

Using the Edit tool, replace the existing No-branch line in Step 5:

Old text:

```
- **No** → say "Samples curation complete. You can invoke this skill again any time to add more."
```

New text:

```
- **No** → proceed to Step 6.
```

- [ ] **Step 4: Append Step 6 after Step 5**

Using the Edit tool, append the following block at the end of the file:

````

## Step 6: Store learnings and close

Review the full curation session — not individual loops — for patterns worth storing.
For each qualifying observation, append one tagged line to `.claude/learnings.md`
(create with standard header if missing):

```text
[cc-content:cc-content-samples-curation] <concise observation> — <YYYY-MM-DD>
````

Qualifies: consistent curation preferences observed across this session (e.g., "client
consistently curates posts under 1,500 characters"); types of content the owner
explicitly rejected as samples (e.g., "client rejected long-form samples as too
time-consuming to read"); quality signals the owner repeatedly cited when annotating.

Does not qualify: individual sample annotations — those are captured in
`context/samples.md`; facts already in any `context/` file or `CLAUDE.md`; facts
semantically equivalent to an existing `.claude/learnings.md` entry under any plugin
tag (when in doubt, skip).

Check for the file before appending:

```bash
ls .claude/learnings.md 2>/dev/null && echo "exists" || echo "missing"
```

Standard header when creating the file:

```markdown
# Learnings

Corrections and feedback collected during content sessions.
Entries are tagged by skill and dated.

---
```

Confirm any entries written: "✓ N learning(s) saved to `.claude/learnings.md`." If no
qualifying observations are found, proceed silently. Then say: "Samples curation
complete. You can invoke this skill again any time to add more."

````

- [ ] **Step 5: Verify**

```bash
grep -n "Step 0\|Step 6\|proceed to Step 6\|Store learnings and close" \
  plugins/cc-content/skills/cc-content-samples-curation/SKILL.md
````

Expected: lines for `## Step 0`, the updated `proceed to Step 6` No-branch, and `## Step 6: Store learnings and close`.

- [ ] **Step 6: Commit**

```bash
git add plugins/cc-content/skills/cc-content-samples-curation/SKILL.md
git commit -m "feat: ✨ add ONSR loop to cc-content-samples-curation"
```

---

### Task 4: cc-content-session-wrap — Add Step 0 and scope Step 5 scan

**Files:**

- Modify: `plugins/cc-content/skills/cc-content-session-wrap/SKILL.md`

- [ ] **Step 1: Read the file**

```bash
cat -n plugins/cc-content/skills/cc-content-session-wrap/SKILL.md
```

Confirm `## Step 1: Review deliverables` and `## Step 5: Detect recurring patterns` are present. Note the exact line where `Parse all entries that follow the` appears inside Step 5.

- [ ] **Step 2: Insert Step 0 before Step 1**

Using the Edit tool, insert the following block immediately before `## Step 1: Review deliverables`:

```
## Step 0: Recall learnings

If `.claude/learnings.md` exists, read it silently. This ensures pattern detection
in Step 5 operates on the most current state of the file. Do not announce this step.
If the file is absent, continue normally.

```

(Leave one blank line between the new block and `## Step 1`.)

- [ ] **Step 3: Add scoping line to Step 5**

Using the Edit tool, insert one line immediately after the `cat .claude/learnings.md 2>/dev/null` bash block in Step 5. The exact insertion point is the line after the closing triple-backtick of that bash block and before the `Parse all entries` line.

Insert:

```
Only scan entries prefixed `[cc-content:` — entries from other plugins are managed
by their own tooling and must not be surfaced or promoted here.

```

Then update the next sentence to match:

Old text:

```
Parse all entries that follow the `[<skill-name>] <text> — <date>` format.
```

New text:

```
Parse the filtered entries that follow the `[cc-content:<skill-name>] <text> — <date>` format.
```

- [ ] **Step 4: Verify**

```bash
grep -n "Step 0\|Only scan entries\|filtered entries" \
  plugins/cc-content/skills/cc-content-session-wrap/SKILL.md
```

Expected: lines for `## Step 0: Recall learnings`, `Only scan entries prefixed`, and `Parse the filtered entries`.

- [ ] **Step 5: Commit**

```bash
git add plugins/cc-content/skills/cc-content-session-wrap/SKILL.md
git commit -m "feat: ✨ add ONSR loop to cc-content-session-wrap"
```

---

### Task 5: cc-content-new-skill — Add Step 0 and update SKILL.md skeleton

**Files:**

- Modify: `plugins/cc-content/skills/cc-content-new-skill/SKILL.md`

- [ ] **Step 1: Read the file**

```bash
cat -n plugins/cc-content/skills/cc-content-new-skill/SKILL.md
```

Confirm `## Step 1: Determine the format name and mode` is present (the skill's own Step 1). Also confirm that inside Step 5's skeleton spec, the lines `**Step 1 — Load context**` and `**Step 8 — Feedback**` are present.

- [ ] **Step 2: Insert Step 0 before the skill's own Step 1**

Using the Edit tool, insert the following block immediately before `## Step 1: Determine the format name and mode`:

```
## Step 0: Recall learnings

If `.claude/learnings.md` exists, read it silently. Apply relevant entries — such as
prior skill-creation observations or project-level defaults that would affect how this
skill is structured. Do not announce this step. If the file is absent, continue normally.

```

(Leave one blank line between the new block and `## Step 1`.)

- [ ] **Step 3: Add Step 0 to the skeleton spec inside Step 5**

In Step 5 of this skill, the skeleton is described as a list of `**Step N — Name**` paragraphs. Using the Edit tool, insert the following paragraph immediately before the `**Step 1 — Load context**` paragraph:

```
**Step 0 — Recall learnings**
At the very start, if `.claude/learnings.md` exists, read it silently. Apply all
relevant entries to inform this run. Do not announce this step. If the file is absent,
continue normally.

```

- [ ] **Step 4: Update Step 8 in the skeleton spec inside Step 5**

Using the Edit tool, replace the existing `**Step 8 — Feedback**` paragraph with:

Old text:

```
**Step 8 — Feedback**
Ask whether the output met expectations. If the user provides a correction, append a tagged,
dated entry to `.claude/learnings.md` (create the file with a header if it doesn't exist).
Confirm with "✓ Feedback saved to `.claude/learnings.md`." If the user confirms quality or
skips, deliver a closing line and exit. Include a `[TODO: ...]` for any format-specific
delivery note (e.g., "Paste into your CMS", "Send for review").
```

New text:

```
**Step 8 — Feedback**
Run the auto-store phase before asking for explicit feedback. Review the run and for each
qualifying observation append one tagged line to `.claude/learnings.md` (create with standard
header if missing): `[cc-content:<skill-name>] <concise observation> — <YYYY-MM-DD>`.
Qualifies: content preferences or constraints not already in loaded context files or
`CLAUDE.md`; corrections the owner made; project-specific facts that would change future
output; accepted/rejected suggestions deviating from best practices. Does not qualify:
standard behavior; facts in context files or `CLAUDE.md`; semantically equivalent entries
already in `.claude/learnings.md` under any tag (when in doubt, skip).

Then ask whether the output met expectations. If the user provides a correction, append it as
a tagged entry (same format and criteria). Confirm total: "✓ N learning(s) saved to
`.claude/learnings.md`." If the user confirms quality or skips, confirm any auto-stored
entries, then deliver a closing line and exit. Include a `[TODO: ...]` for any format-specific
delivery note (e.g., "Paste into your CMS", "Send for review").
```

- [ ] **Step 5: Verify**

```bash
grep -n "Step 0\|Recall learnings\|Auto-store phase\|auto-store phase\|Run the auto-store" \
  plugins/cc-content/skills/cc-content-new-skill/SKILL.md
```

Expected: at least three matches — the skill's own Step 0, the skeleton's Step 0 description, and the skeleton's Step 8 auto-store reference.

- [ ] **Step 6: Commit**

```bash
git add plugins/cc-content/skills/cc-content-new-skill/SKILL.md
git commit -m "feat: ✨ add ONSR loop to cc-content-new-skill and update skeleton"
```
